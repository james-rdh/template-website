services:
  nginx:
    image: nginx:latest
    ports:
      - 80:80
      - 443:443
    volumes:
      - ./public/:/var/www/html/
      - ./nginx/:/etc/nginx/
      - ./letsencrypt/:/etc/letsencrypt/
    command: /bin/sh -c "/etc/nginx/init.sh"
    restart: unless-stopped
  certbot:
    image: certbot/certbot:latest
    depends_on:
      - nginx
    volumes:
      - ./public/:/var/www/html/
      - ./nginx/:/etc/nginx/
      - ./letsencrypt/:/etc/letsencrypt/
    command: /bin/sh -c "/etc/letsencrypt/init.sh"